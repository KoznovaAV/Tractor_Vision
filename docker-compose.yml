# Tractor Vision — оркестрация сервисов.
#
# Обучение вынесено из Docker (идёт локально в conda-окружении `tractor`),
# поэтому compose содержит только рантайм-сервисы: API-инференс и прогон тестов.
# Оба используют один CPU-образ.
#
#     docker compose up api
#     docker compose run --rm test
#
# Примечание: version-ключ намеренно опущён — в Compose v2 он устарел и
# вызывает предупреждение.

# Сборка CPU-варианта образа (единственный вариант — GPU нужен только для
# локального обучения, которое в Docker не выполняется).
x-build-cpu: &build-cpu
  context: .
  args:
    TORCH_VARIANT: cpu

services:
  # ---- API-сервис: CPU-инференс ------------------------------------------
  api:
    build: *build-cpu
    image: tractor-vision:cpu
    command: uvicorn src.api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - ./weights:/app/weights:ro
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import urllib.request;
          urllib.request.urlopen('http://localhost:8000/health')
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ---- Тесты: CPU -------------------------------------------------------
  test:
    build: *build-cpu
    image: tractor-vision:cpu
    command: pytest tests/ -v --cov=src
    volumes:
      - ./data:/app/data
      - ./weights:/app/weights
